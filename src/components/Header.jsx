import { Link } from "react-router-dom";

export default function Header(props) {

    return (
        <header>
            <h1>ProGress</h1>
            <nav>
                <button>Join Now</button>
            </nav>
        </header>
    );
}